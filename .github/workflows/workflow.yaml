on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate-yaml:
    runs-on: ubuntu-latest
    steps:
      - name: Check for only .yaml file extension
        uses: batista/lint-filenames@v1.0.1
        with:
          path: 'prod/'
          pattern: "\\.yaml$"

      - name: Check for undescores
        uses: batista/lint-filenames@v1.0.1
        with:
          path: 'prod/'
          pattern: "^_+"
      




